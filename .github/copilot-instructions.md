# Copilot Instructions — VH CC Tweaks

All vhcctweaks coding instructions are in `.github/instructions/vhcctweaks.instructions.md` (loaded via `applyTo: '**'`). This file is intentionally minimal to avoid duplication.
